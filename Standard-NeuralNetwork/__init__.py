import numpy as np
import Initial_NeuralNetwork as INN
x = np.array([[1, 2, 3], [2, 3, 4]])
#x = x.reshape((x.shape[0], x.shape[1]))
def fun(m = [], out_m = []):
    m.clear()
    m.append(i)
y = np.array([[1], [2]])
#fun(y)
x = [[1, 2, 3], [2, 3, 4]]
print(x.shape)
print(y.shape)